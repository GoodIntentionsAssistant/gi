/**
 * User
 */
const dotty = require("dotty");

module.exports = class User {

/**
 * Constructor
 *
 * @param string session
 * @access public
 * @return void
 */
	constructor(session) {
		this.session = session;
  }
  
}