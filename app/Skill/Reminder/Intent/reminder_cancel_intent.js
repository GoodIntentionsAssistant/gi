/**
 * Reminder Cancel Intent
 */
const Intent = require('../../../../src/Intent/intent');
const moment = require('moment');

module.exports = class ReminderCancelIntent extends Intent {

  setup(app) {
    this.train([
    ]);
  }

}