/**
 * Reminder Reschedule Intent
 */
const Intent = require('../../../../src/Intent/intent');
const moment = require('moment');

module.exports = class ReminderRescheduleIntent extends Intent {

  setup(app) {
    this.train([
    ]);
  }

}